var person = {
  name: 'Vishal'
}

person.age = 25;
person.name = 'mike';

console.log(person);


// run file like : node inspect playground/debugging.js
// breakpoints at the first line first
// list(10) lists 10 lines above and below the breakpoint

// n: next line, c: continue

// type repl to go into read evaluate print loop
// type in variable names to get the data

// ctrl+c to exit repl
